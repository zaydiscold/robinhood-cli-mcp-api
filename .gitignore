node_modules/
dist/
.DS_Store
.env
.env.*
!.env.example
fixtures/private/
info/
*.log
*.tgz
capture-extension/
coverage/

# git-crypt keys must stay outside Git history.
git-crypt-key
*.git-crypt-key

# local safety backups (never commit)
_local-backups-*/

# private/working notes — never publish (research sweeps, proofs, machine-setup notes)
research/
proofs/
MOTHERSHIP-SETUP.md

# raw per-account CDP captures hold real account last-4s + types — never commit (keep local copies)
api-map/cdp-capture-*-per-account.json
