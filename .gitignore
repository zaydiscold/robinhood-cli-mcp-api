node_modules/
dist/
.DS_Store
.env
.env.*
!.env.example
fixtures/private/
info/
*.log
*.tgz
capture-extension/
coverage/
__pycache__/
*.py[cod]

# git-crypt keys must stay outside Git history.
git-crypt-key
*.git-crypt-key

# local safety backups (never commit)
_local-backups-*/
/_*.json

# local exact-value denylist for the public portability scan
.private-portability-values

# private runtime output (the rest of local/ is intentionally git-crypt tracked)
local/portfolio-snapshots.jsonl
local/legacy-reports/

# private/working notes — never publish (research sweeps, proofs, local setup notes)
research/
proofs/
*-SETUP.md

# raw per-account CDP captures hold real account last-4s + types — never commit (keep local copies)
api-map/cdp-capture-*-per-account.json

# Local operator records and one-off session helpers. Never publish personal state.
/ball-knowledge.md
/hotlist.md
/rolls.md
/trade-notes.md
/trading-log.md
/inject-token.py
/inject.py
/NONEXISTENT
