# Hotlist — operator-maintained ticker watchlist (the agent reads this on finance tasks)
#
# One ticker per line: `TICKER — optional thesis/note` (em or plain dash; thesis optional).
# Agents: read this alongside ball-knowledge.md on finance tasks; quote it live with the
# `hotlist` command (CLI) or robinhood_hotlist (MCP).
# Lines containing "(example)" are ignored by the parser — replace them with your own.
#
# Zayd Khan // cold // www.zayd.wtf

NVDA — ai capex supercycle; watch for dip entries (example)
QDTE — weekly-income ETF; verify distribution sustainability before sizing up (example)
