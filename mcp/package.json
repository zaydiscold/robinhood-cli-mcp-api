{
  "name": "@zaydiscold/robinhood-cli-mcp",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "MCP server for personal live Robinhood read/write API-map tools.",
  "author": "Zayd Khan // cold (www.zayd.wtf)",
  "bin": {
    "robinhood-cli-mcp": "dist/server.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json && node scripts/ensure-bin-mode.mjs",
    "pretest": "corepack pnpm --filter @zaydiscold/robinhood-cli build && corepack pnpm --filter @zaydiscold/robinhood-cli-mcp build",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "@zaydiscold/robinhood-cli": "workspace:*",
    "zod": "^4.2.0"
  },
  "engines": {
    "node": ">=20"
  }
}
