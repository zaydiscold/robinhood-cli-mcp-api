# Trade Notes — film-study ledger (operator + agent shared)

<!-- Zayd Khan // cold // www.zayd.wtf -->

```
PURPOSE:  Film-study notes attached to trades — study what worked, write down the lesson, revisit
          it next time. `review` (CLI) / robinhood_review (MCP) join these notes onto trades and
          round trips by ref; append with `review note <ref> "<text>"` or robinhood_review_note.
FORMAT:   `### YYYY-MM-DD HH:MM | <ref>` then the note text then `---` (ref = order id, symbol,
          or symbol+date, freeform). Append-only, newest last; committed + operator-facing — keep
          entries generic, same spirit as trading-log.md.
```

### 2026-06-12 00:00 | EXAMPLE-REF (example)

(example) Entry-format demo — a real note might read: "HPE 9/18 $30C: entered on the earnings dip,
closed +$1,500 — thesis was right but I exited two weeks early; next time hold to the roll point."

---
